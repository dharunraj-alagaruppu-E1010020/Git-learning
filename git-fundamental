Welcome git learning

git clone :

    This command is used to clone or copy a repository from a remote source (like GitHub) to your local machine. 

git status :

    It helps you understand the current state of your working directory and staging area.

git add file name :

     This command is used to add changes in a specific file to the staging area.

git commit -m "messages" :

    This command is used to save your changes to the local repository. The -m flag allows you to include a commit message describing the changes you made. 

git push origin main :

    This command is used to push your local changes to the remote repository.

git pull :

    This command is used to fetch and merge changes from a remote repository into your current branch. I

cd .. :

    It is a shell command to change the current working directory to its parent directory. It's used to navigate up one level in the directory structure.
