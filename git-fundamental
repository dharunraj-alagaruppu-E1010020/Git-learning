git clone:
This command clones or copies a repository from a remote source (like GitHub) to your local machine.

git status:
This command helps you understand the current state of your working directory and staging area.

git add [file name]:
This command adds changes in a specific file to the staging area.

git commit -m "message":
This command saves your changes to the local repository. The -m flag allows you to include a commit message describing the changes you made.

git push origin main:
This command pushes your local changes to the remote repository.

git pull:
This command fetches and merges changes from a remote repository into your current branch.

cd ..:
This is a shell command that changes the current working directory to its parent directory. It is used to navigate up one level in the directory structure.

git init:
This command creates a new .git directory in your local repository, initializing it as a Git repository.

git remote add origin [git_repo_link]:
This command adds a remote repository link (referred to as "origin") to your local repository.

git branch:
This command lists all branches in the current repository.

git branch -M main:
This command renames the current branch to main.

git branch -a:
This command lists all branches, including remote-tracking branches, in the current repository.

git checkout [new_branch_name]:
This command switches to a different branch.

git diff [branch_name]:
This command compares changes between the current branch and the specified branch.

git merge [sub_branch]:
This command merges the specified branch into the current branch.

git branch [new_branch_name]:
This command creates a new branch in the current repository.
